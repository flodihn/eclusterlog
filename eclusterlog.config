[
    {eclusterlog, [
        %% Out-comment to make eclusterlog join an existing mnesia database created
        %% on the specified node in the cluster. 
        %{join_db, ecluster_srv@localhost}
    ]}
].
